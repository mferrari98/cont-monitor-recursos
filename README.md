# Monitor de Recursos

UI + API para ver metricas del servidor. Se accede via `/monitor/`.

## Configuracion necesaria
- `MONITOR_API_TOKEN` (opcional).
- `ALLOWED_ORIGINS` (CSV) para CORS.

## Desarrollo
```bash
npm install
npm run dev:all
```
